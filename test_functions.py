from functions import * 

def test_neutral_colors(neutral_colors):
    if input_list is "black"
    assert output is "Neutral color: any color will match even similar shades of that color"
    if input_list is "white"
    assert output is "Neutral color: any color will match even similar shades of that color"
   
def test_comp_colors(comp_colors):
    if input_list is "red"
    assert output is "green"
    if input_list is "green"
    assert output is "red"